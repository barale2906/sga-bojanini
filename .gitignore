# Solo se ignoran datos sensibles y generados.
# Todo lo demás (docker/, Makefile, etc.) se versiona.

# Entorno sensible (nunca versionar)
src/.env

# Dependencias generadas (se instalan con make init)
src/vendor/
src/node_modules/

# Logs
src/storage/logs/

# IDEs y herramientas locales
.cursor/
.idea/
*.iml
